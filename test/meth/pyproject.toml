[project]
authors = [{name = "Nils Homer", email = "nilshomer@gmail.com"}]
dependencies = ["toolshed>=0.4.8,<0.5"]
name = "meth"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["osx-arm64"]

[tool.pixi.pypi-dependencies]
meth = { path = ".", editable = true }

[tool.pixi.tasks]
